<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              Visual="Material"
              Shell.NavBarHasShadow="False"
             x:Class="GymCRM.Views.ModifyPackagePage">
    <ContentView.Content>
        <StackLayout Spacing="20">
            <Entry Placeholder="Package Name" Text="{Binding SelectedPackage.Name}"  />
            <Entry Placeholder="Months" Text="{Binding SelectedPackage.Months}" />
            <Entry Placeholder="Amount" Text="{Binding SelectedPackage.Amount}" />
        </StackLayout>
    </ContentView.Content>
</ContentView>